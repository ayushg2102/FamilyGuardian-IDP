# Environment Configuration for FamilyGuardian-IDP

# Development API URL (used when NODE_ENV !== 'production')
VITE_API_BASE_URL=http://172.172.233.44:9000/api

# Production API URL (used when deployed on Vercel)
# This will be proxied through Vercel's routing
VITE_API_BASE_URL_PROD=/api

# Application Environment
NODE_ENV=development

# Optional: If you need to override the API URL in any environment
# VITE_OVERRIDE_API_URL=your-custom-api-url
